server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;
    location /health {
        return 200 "healthy";
        add_header Content-Type text/plain;
    }
    location / {
        try_files $uri $uri/ /index.html;
    }
}
